{% extends 'base.html.twig' %}
{% block title %}{{ title }}{% endblock %}
{% block body %}

    </br>
    <table class="table table-bordered text-center" style="width:50%;margin-left:25%">
        <center><label>News</label></center>

        <tr class="active">
            <th class="text-center">п№</th>
            <th class="text-center">id записи</th>
            <th class="text-center">Заголовок записи</th>
            <th class="text-center">Дата записи</th>
            <th></th>
        </tr>

        {% for post in news %}

        <tr class="success">
            <td >{{ loop.index }}</td>
            <td>{{ post.id }}</td>
            <td>{{ post.title }}</td>
            <td>{{ post.date| date ('d.m.Y-H:i') }}</td>
            <td><a href="{{ url('curr', {'id': post.id}) }}">Show record</a></td>
        </tr>

{% endfor %}
    </table>

{% endblock %}